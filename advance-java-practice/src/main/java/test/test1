Group BY
Order BY
WHERE
HAVING
FROM
SELECT

SELECT * FROM ORDER WHERE

EMP
empId  name mgrId
1      e1   null
2      e2   1
3      e3   2
4      e4   2

select
IF(gc.count >= 2)
  gc. mgrid, gc.count
from EMP join (select mgrId, count(*) from EMP groupBy mgrId) as gc





1. SELECT
2. FROM
3. WHERE
4. HAVING
5. GROUP BY
6. ORDER BY




